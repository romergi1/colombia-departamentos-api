apiVersion: v1
kind: Service
metadata:
  name: departamentos-api-svc
  namespace: departamentos
spec:
  type: NodePort
  selector:
    app: departamentos-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 30080